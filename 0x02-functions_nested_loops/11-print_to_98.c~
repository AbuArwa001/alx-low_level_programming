#include "main.h"
#include <stdio.h>
/**
 * print_to_98 - print all natural numbers from n to 98
 *
 * @n: a starting poin
 *
 *
 **/
void print_to_98(int n)
{
	int i;
	for (i = n; i <= 98; i++)
	{
		printf("%d", i);
		if (i != 98)
		{
			printf(", ");
			continue;
		}
	}
	printf("\n");
}
