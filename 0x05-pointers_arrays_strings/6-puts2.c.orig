#include "main.h"

/**
 * puts2 - Prints every other character
 * @str: string to be used
 */
void puts2(char *str)
{
	int i = 0;

	while (*(str + i) != '\0')
	{
		if ((((int) *(str + i) + 0) % 2) == 0)
			_putchar(((int) *(str + i) + 0));
		i++;
	}
	_putchar('\n');
}
